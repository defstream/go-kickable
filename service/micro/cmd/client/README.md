# cani.micro.client

go-micro client for the kickable API. Discovers the `Kickable` service via mDNS.

## Usage

```shell
./bin/cani.micro.client -kick "it"
```

| Flag | Default | Description |
|------|---------|-------------|
| `-kick` | `""` | The item to determine as kickable |
