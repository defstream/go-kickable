# cani.http.server

HTTP server for the kickable API.

## Usage

```shell
./bin/cani.http.server -address localhost:3000
```

| Flag | Default | Description |
|------|---------|-------------|
| `-address` | `localhost:3000` | Address to listen on |
